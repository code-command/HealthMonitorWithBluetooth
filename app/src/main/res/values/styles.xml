<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextInputLayoutTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/customcolorAccent</item>
    </style>

    <style name="NormalHint" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/appNormalHint</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="ErrorHint" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/appErrorHint</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
